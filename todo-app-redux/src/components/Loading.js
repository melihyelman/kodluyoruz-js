function Loading() {
  return (
    <div
      style={{
        padding: 15,
        fontSize: 24,
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      Loading...
    </div>
  );
}

export default Loading;
